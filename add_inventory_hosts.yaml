---
- name: Add hosts
  hosts: localhost
  gather_facts: false

  tasks:
        
  - name: Add host
    awx.awx.host:
      name: alfonzo
      description: "Local Host Group"
      inventory: "Test_Demo"
      state: present
      variables:
        example_var: 123
